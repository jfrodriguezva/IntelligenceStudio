# Do not commit a populated .env file.
# SQL Server and API-Football credentials are stored with .NET user-secrets,
# not in this file. See README.md for the commands.

# Used by services/api during later import work. Keep this empty until the
# provider contract has been verified.
API_FOOTBALL_API_KEY=
